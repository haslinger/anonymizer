<p id="notice"><%= notice %></p>

<h1>Aggregate Downloads</h1>

<table>
  <thead>
    <tr>
      <th>Episode</th>
      <th>Day</th>
      <th>Volume</th>
      <th>Hits</th>
      <th>Count</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @downloads.each do |download| %>
      <tr>
        <td><%= download.episode.name %></td>
        <td><%= download.day %></td>
        <td><%= download.volume.to_i / 1048576 %></td>
        <td><%= download.hits %></td>
        <td><%= download.count %></td>
        <td><%= link_to 'Show', download %></td>
        <td><%= link_to 'Edit', edit_download_path(download) %></td>
        <td><%= link_to 'Destroy', download, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Aggregate Download', new_download_path %>
